<h3 class="title">Edit gist</h3>

<%= render "form.html", Map.put(assigns, :action, gist_path(@conn, :update, @gist)) %>

<div class="actions">
  <%= link "Back", to: gist_path(@conn, :index) %>
</div>
